language: python

python: 
  - 3.4

virtualenv:
  system_site_packages: true

before_install:
  - sudo apt-get -qq update
  - sudo apt-get binutils libproj-dev gdal-bin

install:
  - pip install -r requirements.txt

script: 
  - python manage.py test WaldmeisterMap
